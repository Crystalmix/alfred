/*
'use strict';

*/
/* jasmine specs for filters go here *//*


describe('filter', function() {
    beforeEach(module('alfredDirective'));

    describe('FilterConnections Test', function() {
        it('should replace VERSION',
            inject(function(filterConnectionsFilter) {
                //var filterConnections = $filter("filterConnections")
                expect(filterConnectionsFilter([], null, 0, 0)).toEqual([]);
        }));
      });
});
*/
